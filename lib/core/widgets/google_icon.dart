import 'package:flutter/material.dart';

class GoogleIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/Google icon.png', // مسار الأيقونة
      width: 40, // العرض
      height: 40, // الارتفاع
    );
  }
}
